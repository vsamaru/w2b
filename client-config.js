const clientConfig = {
	siteUrl: 'http://w2b.now.sh', // e.g. 'http://w2b.now.sh' - Your WordPress site URL.
	graphqlUrl: 'http://w2b.now.sh/graphql' // e.g. 'http://w2b.now.sh/graphql' - Your WordPress site url followed by '/graphql'
};

export default clientConfig;
